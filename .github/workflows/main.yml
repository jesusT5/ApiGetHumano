name: ApiGetHumano
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello world"
      - name: Pint GoodBye
        run : echo "Goodbye"
